"use client";

import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#f00", // Change later
        },
        secondary: {
            main: "#0f0", // Change later
        },
    },
    typography: {
        fontFamily: "Funnel Sans, sans-serif",
    },
})